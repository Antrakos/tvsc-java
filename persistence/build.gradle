group 'persistence'
dependencies {
    compile project(':core')
    compile spring_context
    compile(spring_orm) {
        exclude module: 'spring-beans'
    }
    compile postgres
    compile groovy
    compile spring_boot_starter_jdbc
}